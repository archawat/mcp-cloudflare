{
  "name": "mcp-cloudflare",
  "type": "module",
  "version": "1.0.0",
  "description": "MCP server for managing Cloudflare DNS records across multiple domains",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
